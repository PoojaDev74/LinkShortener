# TinyLink Frontend

## Setup
1. Install dependencies:
   ```
   cd frontend
   npm install
   ```
2. Dev:
   ```
   VITE_API_BASE=http://localhost:4000 npm run dev
   ```
3. Build for production:
   ```
   npm run build
   ```

Ensure `VITE_API_BASE` points to your backend URL when deploying.
