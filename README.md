# TinyLink - URL Shortener (Full Project)

This archive contains a ready-to-paste TinyLink project (backend + frontend).

**Structure**
```
tinylink_full/
├─ backend/
├─ frontend/
└─ postman_collection.json
```

Follow the READMEs inside `backend/` and `frontend/` to run locally or deploy.

